# ── Frontend / tooling ─────────────────────────────────────────────
# Open Design local tooling (not part of the production frontend)
.od-skills/
# Playwright MCP / browser-automation scratch output
.playwright-mcp/

# ── Secrets / local env (never commit real secrets) ───────────────
.env
app/.env
*.env.local
# Real k8s secret (only the .example template is committed).
k8s/backend/secret.yaml

# ── Python / backend ───────────────────────────────────────────────
.venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# ── Logs / generated ───────────────────────────────────────────────
*.log
*.zip
*.tar
*.tar.gz
*.sqlite3
*.db

# ── IDE / OS ───────────────────────────────────────────────────────
.vscode/
.idea/
.DS_Store
