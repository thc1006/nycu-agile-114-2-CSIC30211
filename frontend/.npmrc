# Record exact versions on install so new deps don't silently float on ^/~ ranges.
# Existing ranges in package.json are pinned by package-lock.json; tighten them to
# exact during the next dependency bump. See supply-chain notes in the PR review.
save-exact=true
